namespace TaskTracker.MVC.Domain.Enums;

public enum UserRole    { Admin = 1, Developer = 2, Viewer = 3 }
public enum TaskStatus  { Pending = 1, InProgress = 2, Completed = 3, Blocked = 4 }
public enum TaskPriority{ Low = 1, Medium = 2, High = 3, Critical = 4 }
